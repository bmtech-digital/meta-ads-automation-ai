# Flow G — Ab decide. 09:30 Asia/Jerusalem.
# Generated by scripts/generate_from_flows.py from config/flows.yaml — do not edit by hand.
apiVersion: batch/v1
kind: CronJob
metadata:
  name: agent-daily-ab-decisions
  namespace: campaigner
spec:
  schedule: "30 9 * * *"
  timeZone: "Asia/Jerusalem"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 5
  startingDeadlineSeconds: 600
  jobTemplate:
    spec:
      backoffLimit: 0
      activeDeadlineSeconds: 1800
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: agent
              image: us-central1-docker.pkg.dev/bemtech-478413/generic-agent-repo/campaigner-agent:latest
              imagePullPolicy: Always
              command: ["bash", "runners/daily_ab_test_decisions.sh"]
              envFrom:
                - secretRef:
                    name: campaigner-secrets
              env:
                - name: GCP_PROJECT_ID
                  value: "bemtech-478413"
                - name: GCP_LOCATION
                  value: "us-central1"
                - name: GOOGLE_APPLICATION_CREDENTIALS
                  value: "/var/secrets/gcp/credentials.json"
              volumeMounts:
                - name: gcp-credentials
                  mountPath: /var/secrets/gcp
                  readOnly: true
              resources:
                requests: { cpu: 150m, memory: 384Mi }
                limits: { cpu: 750m, memory: 1Gi }
          volumes:
            - name: gcp-credentials
              secret:
                secretName: gcp-vertexai-credentials
